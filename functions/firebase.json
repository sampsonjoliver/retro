{
  "functions": {
    "source": "lib",
    "predeploy": ["yarn lint", "yarn build", "yarn copy-deps"]
  },
  "firestore": {
    "rules": "firestore.rules",
    "indexes": "firestore.indexes.json"
  }
}
